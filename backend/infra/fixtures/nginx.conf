server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    default_type text/html;

    location = /health {
        default_type text/plain;
        return 200 "ok\n";
    }

    location / {
        try_files $uri =404;
    }
}
